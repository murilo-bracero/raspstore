version: "3.9"
services: 
  raspstore-api:
    build: raspstore-api
    ports:
      - "9000:9000"
      - "9090:9090"
    env_file:
      - raspstore-api/.env.docker
networks:
  default:
    external: true
    name: raspstore-network
